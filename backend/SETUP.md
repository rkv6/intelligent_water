#!/bin/bash
# Backend Setup Guide - Intelligent Water Monitoring System

echo "🌊 Intelligent Water Monitoring System - Backend Setup"
echo "════════════════════════════════════════════════════════"
echo ""
echo "Prerequisites:"
echo "  1. Node.js 16+ installed"
echo "  2. MongoDB running locally OR MongoDB Atlas account"
echo ""
echo "Step 1: Install Dependencies"
echo "  cd backend"
echo "  npm install"
echo ""
echo "Step 2: Set Up MongoDB"
echo ""
echo "Option A: Local MongoDB (Recommended for Development)"
echo "  • Download from: https://www.mongodb.com/try/download/community"
echo "  • Windows: Use MongoDB Community Server MSI installer"
echo "  • Start MongoDB service: mongod"
echo "  • Keep the MONGODB_URI in .env as: mongodb://localhost:27017/intelligent-water"
echo ""
echo "Option B: MongoDB Atlas (Cloud - Recommended for Production)"
echo "  1. Create account at: https://www.mongodb.com/cloud/atlas"
echo "  2. Create a new cluster (Free tier available)"
echo "  3. Click 'Connect' and copy the connection string"
echo "  4. Store connection string in .env file (never in documentation)"
echo "  5. Replace MONGODB_URI in .env with your Atlas URI"
echo "  6. URL encode special characters (@, :, etc.) in password"
echo ""
echo "Step 3: Configure Environment Variables"
echo "  • Copy .env.example to .env (already done)"
echo "  • Edit .env and set:"
echo "    - MONGODB_URI=your-mongodb-uri"
echo "    - JWT_SECRET=change-this-to-a-random-string"
echo "    - PORT=5000"
echo "    - NODE_ENV=development"
echo ""
echo "Step 4: Run the Backend"
echo "  npm run dev"
echo ""
echo "Expected Output:"
echo "  ✓ MongoDB connected"
echo "  ✓ Server running on http://localhost:5000"
echo "  ✓ Test with: curl http://localhost:5000/api/health"
echo ""
echo "Available API Endpoints:"
echo "  POST   /api/auth/signup"
echo "  POST   /api/auth/login"
echo "  POST   /api/auth/logout"
echo "  GET    /api/user/profile"
echo "  PUT    /api/user/profile"
echo "  GET    /api/user/channel-data/:channelID"
echo "  GET    /api/feedback"
echo "  POST   /api/feedback"
echo "  PUT    /api/feedback/:id"
echo "  GET    /api/admin/users (requires admin role)"
echo "  GET    /api/admin/feedback (requires admin role)"
echo "  GET    /api/admin/user-history/:userId (requires admin role)"
echo ""
echo "Troubleshooting:"
echo "  • MongoDB connection refused: Ensure MongoDB is running"
echo "  • Port 5000 already in use: Kill the process or change PORT in .env"
echo "  • Module not found errors: Run 'npm install' again"
echo "  • JWT_SECRET errors: Make sure JWT_SECRET is set in .env"
echo ""
